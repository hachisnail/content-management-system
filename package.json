{
  "name": "content-management-system",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev": "concurrently -k -t -n API,LANDING,WEB,PORTAL -c blue,cyan,green,magenta \"npm run dev --workspace=api\" \"npm run dev --workspace=landing\" \"npm run dev --workspace=web\" \"npm run dev --workspace=portal\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "@astrojs/react": "^4.4.2",
    "@types/react": "^19.2.13",
    "@types/react-dom": "^19.2.3",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
