import { WipState } from '@/features/shared';

const AccessionSetupPage = () => {
  return (
    <WipState 
      title="Accession Setup" 
      message="Items approved from Intake land here. Curators will assign permanent ID numbers, condition reports, and storage locations before finalizing into Inventory."
    />
  );
};

export default AccessionSetupPage;