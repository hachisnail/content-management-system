import { WipState } from '@/features/shared';

const ArticleListPage = () => {
  return (
    <WipState 
      title="Article CMS" 
      message="This dashboard will allow curators to manage news, educational content, and announcements. Features will include publishing workflows, filtering by tags, and bulk actions."
    />
  );
};

export default ArticleListPage;