import { WipState } from '@/features/shared';

const AppointmentListPage = () => {
  return (
    <WipState 
      title="Appointment Requests" 
      message="A list view to manage pending booking requests from the public site."
    />
  );
};

export default AppointmentListPage;