to do=====================================================

separate admin client from landing page
-make landing page into a static site for better seo and proffesional setup
-admin client is now a standalone web app


done=======================================================

make sure a single file can be associated to multiple table and id (just another use case/edge case)

add styling to unauthorized message in files
add loading spinner styles to datatable and try to resolve flickering ui issue

useRealtimeResource.jsx is having problems with connections
further test rbac functionality
fix UsersDashboard realtime feature not syncing possible problem useRealtimeResource and index js race condition

Implement file handling system
-file serving
-file uploading
-file accessiblity

implement user controll
-change name, contact, role, password, etc.