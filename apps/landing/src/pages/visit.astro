---
import Layout from "../layouts/Layout.astro";
import BookingForm from "../components/BookingForm.jsx";
---

<Layout>
  <section class="min-h-screen flex items-center justify-center py-24 px-6 relative overflow-hidden">
    <div class="absolute inset-0 z-0">
      <div class="absolute inset-0 bg-neutral-900/90 z-10"></div>
      <img src="https://images.unsplash.com/photo-1566127444979-b3d2b654e3d7?q=80&w=2000" class="w-full h-full object-cover grayscale opacity-20" alt="Museum Texture" />
    </div>

    <div class="relative z-10 w-full max-w-6xl grid grid-cols-1 lg:grid-cols-2 gap-16 items-start">
      
      <div class="pt-10">
        <span class="text-museum-gold uppercase tracking-widest text-sm">Secure Entry</span>
        <h1 class="font-serif text-5xl md:text-6xl text-white mt-4 mb-6">Plan Your Visit</h1>
        <p class="text-gray-400 text-lg mb-8 leading-relaxed">
          Museo Bulawan operates by appointment only to preserve the sanctity of our artifacts and provide a focused, educational environment.
        </p>
        
        <div class="border-l-2 border-museum-gold pl-6 py-2 space-y-6">
          <div>
            <h4 class="text-white font-serif text-xl mb-1">General Admission</h4>
            <p class="text-sm text-gray-500">Free for Camarines Norte residents<br/>₱100 for visitors</p>
          </div>
          <div>
            <h4 class="text-white font-serif text-xl mb-1">Hours</h4>
            <p class="text-sm text-gray-500">Tue - Sun: 9:00 AM - 4:00 PM<br/>Closed on Mondays</p>
          </div>
          <div>
            <h4 class="text-white font-serif text-xl mb-1">Photography Policy</h4>
            <p class="text-sm text-gray-500">Strictly prohibited in the Gold Gallery. Allowed without flash in the Main Hall.</p>
          </div>
        </div>
      </div>

      <BookingForm client:load defaultType="general" />

    </div>
  </section>
</Layout>